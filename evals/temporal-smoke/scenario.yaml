description: "Smoke test: temporal assertions verify event ordering and custom hooks execute"
timeout_ms: 600000

config_overrides:
  agents:
    maxConcurrent: 4

startup_actions:
  - command: sd create --title "Write alpha.txt" --type task --priority 2
    description: "Create first task for temporal ordering test"
  - command: sd create --title "Write beta.txt" --type task --priority 2
    description: "Create second task for temporal ordering test"
